import { Component } from "./component";
import { Effect } from "./effects";
export declare function beforeUpdate({ update, dispose }: Component, hook: Effect): {
    update: Effect;
    dispose: Effect;
};
export declare function afterUpdate({ update, dispose }: Component, hook: Effect): {
    update: Effect;
    dispose: Effect;
};
export declare function beforeDispose({ update, dispose }: Component, hook: Effect): {
    update: Effect;
    dispose: Effect;
};
export declare function afterDispose({ update, dispose }: Component, hook: Effect): {
    update: Effect;
    dispose: Effect;
};
