import { Param } from "./param";
export interface TransitionProps<T> {
    time: Param<number>;
    value: Param<T>;
    initial?: T;
    duration: Param<number>;
    delay?: Param<number>;
    easing?: (t: number) => number;
}
export declare function createTransition<T>({ equals, mix }: {
    equals(v1: T, v2: T): boolean;
    mix(v1: T, v2: T, position: number): T;
}): ({ time, value, initial, delay, duration, easing, }: TransitionProps<T>) => Param<T>;
export declare const numberTransition: ({ time, value, initial, delay, duration, easing, }: TransitionProps<number>) => Param<number>;
